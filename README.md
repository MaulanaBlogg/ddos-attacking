# # ddos-attacking #
# ddos website & web #

$ pkg update && pkg upgrade
$ pkg install python
$ pkg install git
$ pkg install bash
$ git clone https://github.com/MaulanaBlogg/ddos-attacking
$ cd ddos-attacking
$ ls
$ sh install.sh-attacking
